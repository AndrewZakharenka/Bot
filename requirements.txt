# Voice Bot — локальный стек
# Установка: pip install -r requirements.txt

openai-whisper      # STT: распознавание речи
sounddevice         # запись с микрофона
scipy               # сохранение WAV
numpy               # нужен whisper и sounddevice
requests            # HTTP к Ollama API
pyttsx3             # TTS: синтез речи
customtkinter       # современный UI поверх tkinter
imageio-ffmpeg      # ffmpeg бинарник для Whisper (без системной установки)
